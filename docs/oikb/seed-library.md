# OIKB Seed Library

Migration `20260725_0010` installs ten active, provisional, shared-core definitions:

- `SHARED.FINANCE.OUTSTANDING_BALANCE`
- `SHARED.FINANCE.PAYMENT_VARIANCE`
- `SHARED.OPERATIONS.THROUGHPUT`
- `SHARED.OPERATIONS.BACKLOG`
- `SHARED.OPERATIONS.ABSOLUTE_VARIANCE`
- `SHARED.OPERATIONS.PERCENTAGE_VARIANCE`
- `SHARED.QUALITY.DEFECT_RATE`
- `JOB_TO_CASH.REVENUE.COMPLETED_NOT_INVOICED`
- `JOB_TO_CASH.REVENUE.UNBILLED_REVENUE`
- `MOBILITY.FUEL.FUEL_VARIANCE`

Each uses only an existing WP-2.07 primitive and includes an input contract, output/unit
contract, Trust/readiness requirements, evidence requirement, deterministic validation
cases, approval, activation metadata, fingerprint, and one Intel4Ops provenance source.
The source authority is `intel4ops_proprietary`; quality remains `provisional`. No
regulatory or international-standard authority is claimed.

The seed set proves governance and execution plumbing. It is not a bulk import of the
formula inventory and does not replace domain-owner review before promotion to higher
quality levels.
