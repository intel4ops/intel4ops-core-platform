# Domain Model

Assets, components, failure events, maintenance events, exposure, observation windows, lifecycle
states, censoring, and peer cohorts are governed references. Reliability tables retain analytical
summaries and fingerprints rather than duplicating canonical operational records.
