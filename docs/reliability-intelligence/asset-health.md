# Asset Health

Asset health is an explainable 0–100 composite with normalized inputs, weights, contributions,
coverage, confidence, trend, band, and limitations. Weights must sum to one. Missing components
reduce coverage and cannot be silently imputed.
