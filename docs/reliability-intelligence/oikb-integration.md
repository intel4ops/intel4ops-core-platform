# OIKB Integration

Only an active governed definition whose analytical level is `reliability` may execute. The
definition version, method/version, failure definition, exposure basis, censoring policy, trust
requirement, and readiness requirement form an immutable execution package fingerprint. Shared
definitions and organization specializations use the existing OIKB resolution and tenancy rules.
