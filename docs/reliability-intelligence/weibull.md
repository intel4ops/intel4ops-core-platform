# Weibull Reliability

The bounded two-parameter Weibull method uses a deterministic probability-plot least-squares fit.
It requires at least three positive, non-constant observed failures and returns shape, scale, B10,
B50, sample diagnostics, censor counts, and limitations. It refuses invalid or insufficient fits.
Censored observations are reported but are not yet included in likelihood estimation.
