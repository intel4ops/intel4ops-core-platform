# Reliability Readiness

Execution requires a completed tenant trust assessment and a persisted `reliability` readiness
decision in `ready` or `ready_with_warnings` status. The method's minimum assets, failures,
exposure, supported exposure basis, lifecycle state, censoring contract, stable units, and active
OIKB version are checked separately. Blocking reasons are structured and no model is forced.
