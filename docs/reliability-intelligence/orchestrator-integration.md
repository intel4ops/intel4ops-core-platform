# Orchestrator Integration

Reliability uses the existing `reliability` analytical level. Progressive sequencing is arithmetic,
statistical, forecasting only where required, then reliability. Each prerequisite preserves its
execution and evidence reference. Unsupported methods fail explicitly, and reliability outputs
remain finding candidates until Findings governance approves publication.
