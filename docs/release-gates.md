# Release Gates

The seeded gates are core platform, migrations, tenancy, authorization, industry packs,
golden scenarios, commercial enforcement, model governance, security, resilience,
observability, and release readiness. Mandatory missing or blocked gates produce
`blocked`; non-waivable failures produce `rejected`; all passes produce `certified`.
Only an eligible waiver can produce `conditionally_certified`.
