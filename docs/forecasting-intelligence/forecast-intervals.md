# Forecast intervals

Forecast points may carry 80, 90, or 95 percent uncertainty intervals. WP-2.12 uses a bounded
empirical absolute-residual interval when at least two residuals exist. Otherwise the interval
is explicitly unavailable. Bounds are uncertainty ranges, not guaranteed outcomes.
