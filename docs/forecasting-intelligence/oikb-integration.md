# OIKB integration

Only active `FORECASTING` definitions execute. The migration seeds twelve provisional,
validated definitions with deterministic cases, method candidates, naïve benchmark,
rolling-origin backtesting, WAPE, interval levels, conservative missing/partial/outlier
policies, Trust and readiness requirements, evidence requirements, provenance, and limitations.
Execution packages are immutable and fingerprinted.
