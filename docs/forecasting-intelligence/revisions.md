# Revisions

A revision links a prior execution to a new immutable execution and records its reason, type,
absolute and percentage change, and method, data, readiness, parameter, and scenario change
flags. Prior forecasts are never overwritten. Equivalent immutable execution requests reuse
the completed execution identified by the reproducibility fingerprint.
