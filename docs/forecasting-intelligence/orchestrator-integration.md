# Orchestrator integration

`FORECASTING_INTELLIGENCE_ENGINE` version 1.0 registers at analytical level `FORECASTING`
with capability `bounded_forecasting_intelligence`. The orchestrator resolves OIKB, validates
Trust and forecasting readiness, records selection and execution trace, and bridges the result
to the common analytical output contract. Unsupported methods remain explicit.
