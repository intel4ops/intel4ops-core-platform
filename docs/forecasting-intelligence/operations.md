# Operations

Apply schema changes only with Alembic. Monitor execution status, duration, candidate counts,
backtest folds, forecast horizon, interval availability, accuracy drift, and bias. Requests are
bounded to 10,000 observations, 12 candidates, 120 horizons, and 20 folds. Use a disposable
PostgreSQL database and the documented confirmation flag for migration lifecycle testing.
